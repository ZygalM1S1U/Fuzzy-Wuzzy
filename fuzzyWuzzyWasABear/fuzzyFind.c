#include "fuzzyFind.h"

char nameReturn()
{

  char localNameBuffer[] = "";


}

char fuzzyGetName(char* nameBuffer)
{
  ///Open a file that has a dictionary of names and maybe even a location in flash where they are stored.
  ///Each name must have a designated space
}
